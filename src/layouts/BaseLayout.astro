---
import BaseHead from "../components/BaseHead.astro";
const props = Astro.props;

interface Props {
  title: string;
  description: string;
}
---

<!doctype html>
<html lang="en">
  <BaseHead {...props} />
  <body
    class="flex selection:bg-amber-800 selection:text-amber-50 flex-col items-center justify-center bg-[#faf9f5]"
  >
    <slot />
    <!--
      <div
        class="fixed print:hidden bottom-5 left-5 z-50 flex items-center rounded-full bg-amber-950 px-2.5 py-1 font-mono text-xs font-semibold text-amber-50"
      >
        <span class="sm:hidden">XS</span>
        <span class="hidden sm:max-md:inline">SM</span>
        <span class="hidden md:max-lg:inline">MD</span>
        <span class="hidden lg:max-xl:inline">LG</span>
        <span class="hidden xl:max-2xl:inline">XL</span>
        <span class="max-2xl:hidden">2XL</span>
      </div>
    -->
  </body></html
>
